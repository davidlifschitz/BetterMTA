/**
 * Default create-client binding (fixture). Live builds replace this file via
 * next.config resolve.alias → create-client.live.ts.
 */
export { createApiClient } from "@/lib/api/create-client.fixture";
