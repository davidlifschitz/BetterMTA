# BetterMTA Release Gate Report (Phase 11 + 12A vocabulary)

**Branch:** `agent/integration-live`  
**Date:** 2026-07-31  
**Contract version:** `2026-07-30`  
**Authoritative machine checklist:** `benchmarks/reports/release-gate-latest.md` (regenerated by `npm --prefix benchmarks/runner run gate`)  
**Go/no-go handoff:** `.agents/handoffs/integration-live.md`  
**Controlled-alpha deploy decision:** ADR-0021  
**Remote validation evidence:** `docs/alpha/REMOTE_VALIDATION.md` (20 tests — executed 2026-07-31)  
**Operating findings log:** `docs/alpha/CONTROLLED_ALPHA_LOG.md` (Controlled Alpha Review 1 evidence)  
**`main` merges:** PR #3 @ `90b6462` (certified alpha **not** redeployed); PR #4 @ `aee63e1` (residuals `FU-NPM-01`, `FU-ALPHA-01`, `FU-ALPHA-02`)

## Status vocabulary

| Status | Meaning |
|---|---|
| `BLOCKED` | Not ready for any remote cohort; outstanding gates or missing evidence |
| `READY_FOR_CONTROLLED_ALPHA` | Phase 12A go outcome: self-hosted Cloudflare Tunnel + Access path proven for allowlisted testers (ADR-0021). **Not** cloud private/public beta |
| `READY_FOR_PRIVATE_BETA` | Hosted Fly private-beta cohort (ADR-0012) ready |
| `READY_FOR_PUBLIC_BETA` | Public beta ready (narrow MVP + all required gates) |

Do not equate controlled alpha with private/public beta.

## Final status

```text
READY_FOR_CONTROLLED_ALPHA
```

**Bounded controlled alpha only.** Origin is a **self-hosted macOS** computer. Tunnel runs through a **user LaunchAgent** (`com.bettermta.cloudflared-alpha`, credentials-file, KeepAlive + RunAtLoad). Availability depends on home power, internet, host wakefulness, Docker/Colima, and user login. This is **not** cloud-grade and **not** public-beta readiness. Intended hosted private/public beta remains Fly.io (ADR-0012) — do **not** label `READY_FOR_PRIVATE_BETA` / `READY_FOR_PUBLIC_BETA`. No competitor-performance claims. OTP candidate-diversity risk remains open.

Certification evidence (2026-07-31): local smoke **8/8**; official remote monitor **6/0**; approved+denied Access interactive auth **PASS** (host evidence, no emails in Git); distinct-digest rollback **PASS** (~105 s); reliability restarts + Colima + LaunchAgent **PASS**; Mac logout/reboot still **PENDING_USER** (explicit residual, non-blocking given honesty docs).

### Phase 12A.13 + certification reviews

| Review | Result | Notes |
|---|---|---|
| Public exposure / Cloudflare Access | **PASS** | Deny-default Access; unauth → login; invalid token blocked; OTP GraphQL/internal/metrics not exposed; no port-forward |
| Secrets / privacy | **PASS** | Host env mode `600`; PlaceRef-only smoke; no secrets/hostname/emails in docs commits |
| Rollback integrity | **PASS** | Distinct web digest LABEL certification image → previous immutable tag; volumes retained |
| Restart / persistence | **PASS*** | Container + Colima + LaunchAgent drills green; Mac reboot/logout deferred with residual risk |
| Release-gate honesty | **PASS** | Status flipped only after remote monitor + Access + distinct rollback evidence |

No open Critical/High findings at go decision.

## Controlled-alpha gates (Phase 12A — ADR-0021)

| ID | Gate | Current | Notes |
|---|---|---|---|
| CA01 | Local compose origin healthy (data+OTP+API+web) | **PASS** | Re-verified 2026-07-31 certification: `start-alpha` + edge live/ready |
| CA02 | Edge proxy in front of compose origin | **PASS** | `smoke-edge.sh` **8/8**; Caddy `127.0.0.1:8088` |
| CA03 | Cloudflare Tunnel (no router port forward) | **PASS** | Named tunnel; credentials-file; canonical user LaunchAgent; exactly one process |
| CA04 | Cloudflare Access deny-by-default + email allowlist + OTP/PIN | **PASS** | Approved + denied interactive auth evidenced on host; emails not in Git |
| CA05 | Remote smoke via Access (allowlisted) | **PASS** | `docs/alpha/REMOTE_VALIDATION.md` table filled; official remote monitor + route complete match |
| CA06 | Availability / ops honesty documented | **PASS** | ADR-0021 + HOST/TUNNEL/ACCESS + drills; LaunchAgent documented as canonical |
| CA07 | Existing correctness / security / privacy / data-honesty / fixture-exclusion / rollback gates not waived | **BINDING** | Still binding; fixtures/privacy PASS |
| CA08 | Controlled-alpha release/rollback (immutable image refs) | **PASS** | Distinct-digest rollback proven 2026-07-31 (`rollback-release.sh` + certification web LABEL image) |
| CA09 | External health monitor (Access service token) | **PASS** | Official `MONITOR_MODE=remote` **6/0**; GH workflow still soft until repo secrets enabled |
| CA10 | Reliability drills | **PASS*** | Service/Colima/LaunchAgent/RT-pause PASS; Mac logout/reboot PENDING_USER residual |
| CA11 | Performance evidence | **PASS*** | Local + remote Access n=15 samples in `docs/alpha/PERFORMANCE.md`; not G15 SLO; no competitor claims |

## G01–G20 summary (honest)

Merge-blocking rows are G01–G07 only. Fly BLOCKED / Google NOT_CLAIMED do not fail the fixture gate exit code alone.

| ID | Status | Merge-blocking | Notes |
|---|---|---|---|
| G01 | **PASS** | yes | Case schema validation; 30 corpus cases / 20 release-subset |
| G02 | **PASS** | yes | Release-subset merge-blocking invariants; rankingPasses=20 |
| G03 | **PASS** | yes | Zero topology-invalid itineraries on subset (D.1) |
| G04 | **PASS** | yes | Zero selected-line accounting errors (D.2) |
| G05 | **PASS** | yes | Ranking coverage (complete before partial / max-sat-before-time) |
| G06 | **PASS** | yes | Deterministic fingerprint order on subset |
| G07 | **PASS** | yes | Honest `dataMode` labeling (B.1 / B.2) |
| G08 | **PASS*** | no | Self-test run separately Phase 11: 6/6 expected fails + soft-subset policy (*gate process marks PENDING until re-executed inside gate) |
| G09 | **PASS** | no | 8 `recorded_data` NYC responses in corpus |
| G10 | **PASS** | no | Live HTTP SUT adapter present |
| G11 | **PASS*** | no | Live smoke Carroll→Bryant verified Phase 9 against compose `:8080` (~2.1s, complete/stale); *fixture gate marks PENDING when API down |
| G12 | **PASS*** | no | Shadow report written (`live-shadow-2026-07-30T19-22-40-758Z.*`); `humanValidity=pending_review` |
| G13 | **NOT_MEASURED** | no | Core workflow a11y (D.3) — Frontend ownership |
| G14 | **PARTIAL** | no | CI contracts + directory-guarded jobs; full app CI additive |
| G15 | **PARTIAL** | no | Beta-load p95 still open. Local n=15 p50=42 ms p95=1170 ms; remote Access n=15 p50=138 ms p95=641 ms max=2700 ms; 0 failures; `dataMode=live` — `docs/alpha/PERFORMANCE.md`. **No** Google comparison; not an SLO |
| G16 | **PARTIAL** | no | Health/locked endpoints proven on local compose; not re-probed by gate process |
| G17 | **BLOCKED** | no | Fly.io not activated — no `flyctl` / credentials / apps |
| G18 | **PENDING** | no | One-action Fly rollback drill (E.4) blocked on G17 |
| G19 | **PENDING** | no | Production alerts + rate-limit ops binding |
| G20 | **NOT_CLAIMED** | no | No Google/Apple/Citymapper superiority evidence |

## Live smoke (Carroll → Bryant)

| Field | Value |
|---|---|
| Case | `bmc-live-smoke-f-carroll-bryant` |
| OD | `st:F21` → `st:D16` (Carroll St → 42 St-Bryant Pk) |
| Lines | `[F]` |
| Result | complete match; 3 constrained + 3 baseline |
| Latency | ~2107 ms |
| Modes | `dataMode=stale`; static `mta-subway-c9c3366cdd16` |
| Human review | `pending_review` |

## Cost ballpark

| Environment | Estimate | Source |
|---|---|---|
| Local compose | Host/Colima only | `docs/RUNBOOKS.md` |
| Intended Fly private beta (no Postgres) | **~$30–50/mo** | `docs/RUNBOOKS.md` |
| Fly proposal (alt framing) | ≈ $25–45/mo; ≈ $55–85/mo w/ Managed PG | `docs/proposals/infrastructure-adr-0005-platform.md` |

## Unblocks required for `READY_FOR_PRIVATE_BETA`

Stage D preparation on `codex/stage-d-private-beta-prep` adds a tested candidate for encrypted cross-replica PlaceRefs, read-only Fly preflight, prior-image manifest capture, guarded image-based rollback, and draft privacy/support operations. It has **not** activated Fly or changed this report's status.

1. Owner-approve and merge the prerequisite Stage C/Stage D branches; provision Fly authentication, apps, volumes, and secrets without exposing values.
2. Run the read-only preflight, deploy immutable commit-labeled images with a non-localhost public HTTPS API origin, and record the exact release set.
3. Complete an image-based rollback drill to the recorded prior set, restore the candidate, and retain evidence (G18 / E.4).
4. Bind aggregated production logs/metrics and minimal alerts (G19); keep API at one replica until a shared rate limiter is proven.
5. Owner/legal-approve the privacy policy, retention configuration, private support channel, and response workflow.
6. Human-accept shadow validity and decide how the five soft multi-line diversity/timeout watch cases bound the invite cohort.
7. Re-run the release gate, live smoke, privacy checks, and public health checks against the Fly URL before any cohort expansion.

Public beta additionally needs G13/G15 closure, production domain/TLS, and explicit non-claims discipline (G20).

## Public-beta readiness preparation

The `codex/stage-f-readiness-harness` candidate adds a bounded, privacy-safe
route-search load probe, a fail-closed ten-gate evidence validator, CI-backed
live-mode fixture exclusion and core-flow Playwright coverage, draft incident
operations, and explicit limitations copy. Evidence is bound to an exact
release commit and SHA-256 artifact hashes; structure-only CI cannot emit a
ready status. See `docs/public-beta/READINESS.md` and
`infra/public-beta/README.md`.

The stacked `codex/stage-f-public-surface` candidate makes the draft
limitations discoverable at `/limitations` and adds app-owned, request-nonce
CSP plus baseline security headers. Local production E2E verifies fresh nonces,
the header contract, the linked limitations route, and no CSP console errors.
This is candidate evidence only; the nonce-rendered shell still needs preview
capacity/load validation and the approved public edge still needs TLS and
runtime-header verification.

The further stacked `codex/stage-f-public-origin-verifier` candidate adds a
commit-bound, privacy-safe, read-only verifier for the approved web/API origins.
It fails closed on insecure or unconfirmed remote targets, redirects, oversized
bodies, missing/weak headers, reused nonces, limitations-copy drift, and API
health/status failures. Local tests exercise the mechanics only; no remote
target was contacted and no public-origin gate was closed.

The further stacked `codex/stage-f-preview-container` candidate adds a CI-only
production-container preview. It builds `apps/web/Dockerfile`, binds the image
to runner-local loopback, runs the 14 mocked-live core-flow/header/accessibility
checks against the container, and retains commit/image-bound privacy-safe
evidence. GitHub CI run `30956573677` passed all ten jobs and its preview
artifact matches approved branch commit `9f10e50`, reports passing smoke, and
records no production mutation or external reachability. This is not a
hosted/public preview and does not prove cloud-platform, edge, external
reachability, capacity, or rollback behavior.

The further stacked `codex/stage-f-accessibility-evidence` candidate writes a
commit-bound automated artifact after the existing keyboard/mobile/axe suite
passes and provides a human review protocol for the same release commit. The
automated result is intentionally `AUTOMATED_PASS_HUMAN_PENDING` and ineligible
for gate passage. Initial PR #14 CI run `30957843171` passed all ten jobs and its
artifact matched implementation commit `c27602b`, the fixed check list, pending
human status, and privacy/no-mutation contract. The latest head audit remains
recorded on PR #14. No human accessibility review has occurred and this
candidate does not close the accessibility gate or change release status.

The further stacked `codex/stage-f-incident-evidence` candidate writes a
commit-bound playbook-readiness artifact after structure validation and adds a
restricted human tabletop protocol for the same release commit. Automated
output is intentionally `PLAYBOOK_PASS_ROTA_DRILL_PENDING`, leaves rota,
channel, and drill status pending, and is ineligible for gate passage. The
authoritative latest-head CI audit is retained in draft PR #15 comments. No
on-call rota or channel was activated, no tabletop or live incident action ran,
and the incident-response gate remains open.

These candidates do **not** close hosted private beta, load/p95, preview,
live rollback, human accessibility, approved incident rota/tabletop, public origin/TLS,
privacy/support approval, publication, or claims-review gates. No deployment,
cohort change, or status change occurred. Current public-beta status is
`NOT_READY`.
